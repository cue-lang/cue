# File starting with .
exec cue eval .foo.cue
cmp stdout expect-stdout

# File starting with _
cp .foo.cue _foo.cue
exec cue eval _foo.cue
cmp stdout expect-stdout

-- expect-stdout --
a: 42
-- .foo.cue --
package foo

a: 42
