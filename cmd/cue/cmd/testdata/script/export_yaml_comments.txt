cue export --preserve-comments --out yaml ./hello
cmp stdout expect-stdout
-- expect-stdout --
# cloud-config
foo: bar
hello:
  # occupy
  world: from mars?
-- hello/hello.cue --
package hello

//cloud-config
foo: "bar"

hello: {
        // occupy
        world: "from mars?"
}
