# simplify works in package mode
exec cue fmt --simplify ./pkg/...
cmp pkg/file.cue file.golden

# simplify works in --recurse mode
exec cue fmt --simplify --recurse ./pkg/
cmp pkg/file.cue file.golden

-- recurse/file.cue --
"quoted": b
-- pkg/file.cue --
"quoted": b
-- file.golden --
quoted: b
