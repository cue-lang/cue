! cue eval foo.cue
cmp stderr expect-stderr

! cue export foo.cue
cmp stderr expect-stderr

! cue fmt foo.cue
cmp stderr expect-stderr

-- expect-stderr --
missing ',' in struct literal:
    ./foo.cue:1:8
-- foo.cue --
"x": 3 for x in a
