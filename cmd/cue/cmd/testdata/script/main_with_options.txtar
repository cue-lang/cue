# Test that MainWithOptions passes context options through
# to the cue context, in this case an injector.

exec cue_inject export in.cue
cmp stdout expect-stdout

-- in.cue --
@extern(testinject)

package p

x: _ @testinject()

-- expect-stdout --
{
    "x": "hello from injector"
}
