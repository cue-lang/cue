env CUE_EXPERIMENT=modules
! exec cue mod publish v1.0.0
env-fill want-stderr
cmp stderr want-stderr
-- want-stderr --
git VCS not found in any parent of "$WORK"
-- cue.mod/module.cue --
module: "x.example@v1"

source: kind: "git"
