env CUE_REGISTRY=malformed!registry@url
! exec cue export .
cmp stderr expect-stderr

-- expect-stderr --
test.org@v0: import failed: cannot find package "example.com/e": cannot fetch example.com/e@v0.0.1: bad value for registry: invalid registry "malformed!registry@url": invalid host name "malformed!registry@url" in registry:
    ./main.cue:2:8
-- main.cue --
package main
import "example.com/e"

e.foo

-- cue.mod/module.cue --
module: "test.org"
language: version: "v0.14.0"
deps: "example.com/e": v: "v0.0.1"
