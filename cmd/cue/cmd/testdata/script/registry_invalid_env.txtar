env CUE_EXPERIMENT=modules
env CUE_REGISTRY=malformed!registry@url
! exec cue eval .
cmp stderr expect-stderr

-- expect-stderr --
cannot make OCI client: invalid host "malformed!registry@url" (does not correctly form a host part of a URL)
-- main.cue --
package main
import "example.com/e"

e.foo

-- cue.mod/module.cue --
module: "test.org"
deps: "example.com/e": v: "v0.0.1"
