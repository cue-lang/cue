# TODO: this evaluation should work OK.
! exec cue eval .
cmp stderr expect-stderr
-- expect-stderr --
import failed: cannot determine source directory for package "main.org/foo/bar": cannot get absolute path for FS of type os.dirFS:
    ./foo.cue:2:8
-- cue.mod/module.cue --
module: "main.org/foo@v0"
-- foo.cue --
package foo
import "main.org/foo/bar"
bar
-- bar/bar.cue --
package bar
x: 5

-- _registry/example.com_v0.0.1/cue.mod/module.cue --
module: "example.com@v0"

-- _registry/example.com_v0.0.1/top.cue --
package main
