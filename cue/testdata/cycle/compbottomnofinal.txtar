// Issues: #667, #695, #622
-- in.cue --
import (
	"strconv"
	"regexp"
)

// In these test, it is impossible to determine the existence of some arcs due
// to mutual dependence on becoming concrete.
//
// This tests shows the essences of when an existence check cannot be resolved.
minimal: {
	a: {
		if b.port == _|_ {
			port: ""
		}
		// Pushdown produces cycle error instead of incomplete.
		@test(err, code=cycle|incomplete)
	}

	b: {
		if a.port == _|_ {
			port: ""
		}
		// Pushdown produces cycle error instead of incomplete.
		@test(err, code=cycle|incomplete)
	}
}

small: {
	#userHostPort: #"^(:(?P<port>\d+))?$"#

	p1: {
		@test(permute, count=2)
		#Y: regexp.FindNamedSubmatch(#userHostPort, #X.port)

		#X: {
			if #Y.port == _|_ {
				port: ""
			}
			if #Y.port != _|_ {
				port: ":" + strconv.FormatInt(#Y.port, 10)
			}
		}
		// Yield-on-running heuristic: the comp's referenced node
		// #Y is not currently being processed (only scheduled), so
		// the comp does not yield and instead reports a cycle error.
		@test(eq, {
			#Y: _|_ @test(err, code=cycle|incomplete)
			#X: _|_ @test(err, code=cycle|incomplete)
		})
	}
}

medium: {
	#userHostPort: #"^(:(?P<port>\d+))?$"#

	p1: {
		@test(permute, count=6)

		#Y: regexp.FindNamedSubmatch(#userHostPort, #X.port) @test(err, code=incomplete|cycle)

		// Yield-on-running heuristic: the comp's referenced node Y
		// is not currently being processed, so the comp does not
		// yield and instead reports a cycle error.

		Y: {
			if #Y.port != _|_ {
				port: strconv.Atoi(#Y.port)
			}
		} @test(err:todo, code=incomplete|cycle)

		#X: {
			// Can never determine whether Y.port exists as its resolution
			// depends on #Y becoming finalized, which, in turn, depends on #X
			// becoming finalized.
			if Y.port == _|_ {
				port: ""
			}
			if Y.port != _|_ {
				port: ":" + strconv.FormatInt(Y.port, 10)
			}
		} @test(err, code=incomplete|cycle)
	}
}

large: {
	#userHostPort: #"^((?P<userinfo>[[:alnum:]]*)@)?(?P<host>[[:alnum:].]+)(:(?P<port>\d+))?$"#

	p1: {
		@test(permute, count=120)
		// Pushdown improvement: #Y, #X, and X now resolve successfully.
		// Y gets a cycle error because #Y.port changed after evaluation.
		// Permute disabled: pushdown introduces ordering sensitivity.

		Y: {
			userinfo: "user"
			host:     "mod.test"
		}

		X: #X.userinfo + #X.host + #X.port @test(err, any, code=incomplete|cycle)

		#X: {
			if Y.userinfo == _|_ {
				userinfo: ""
			}
			if Y.userinfo != _|_ {
				userinfo: Y.userinfo + "@"
			}

			host: Y.host

			if Y.port == _|_ {
				port: ""
			}
			if Y.port != _|_ {
				port: ":" + strconv.FormatInt(Y.port, 10)
			}
		} @test(err, any, code=incomplete|cycle)

		Y: {
			if #Y.userinfo != _|_ {
				userinfo: #Y.userinfo
			}

			host: #Y.host

			if #Y.port != _|_ {
				port: strconv.Atoi(#Y.port)
			}
		} @test(err, any, code=incomplete|cycle)

		#Y: regexp.FindNamedSubmatch(#userHostPort, X) @test(err, any, code=incomplete|cycle)
	}
}
-- out/errors.txt --
[incomplete] minimal.a.port: cyclic reference to field port:
    ./in.cue:12:3
[incomplete] minimal.b.port: cyclic reference to field port:
    ./in.cue:20:3
[incomplete] small.p1.#Y: undefined field: port:
    ./in.cue:33:50
[incomplete] small.p1.#X.port: cyclic reference to field port:
    ./in.cue:36:4
[incomplete] medium.p1.#Y: undefined field: port:
    ./in.cue:59:50
[incomplete] medium.p1.#X.port: cyclic reference to field port:
    ./in.cue:75:4
[incomplete] large.p1.Y.host: undefined field: host:
    ./in.cue:124:13
[incomplete] large.p1.X: undefined field: port:
    ./in.cue:99:33
[incomplete] large.p1.#X.port: cyclic reference to field port:
    ./in.cue:111:4
[incomplete] large.p1.X: undefined field: port:
    ./in.cue:99:33
-- out/todo.txt --
minimal.a/b: pushdown reports cycle error (matching v2) instead of incomplete (v3).
small.p1: pushdown resolves #Y to {port: ""} (matching v2); #X still has cycle error.
medium.p1: pushdown resolves #Y and #X; Y gets cycle error.
large.p1: pushdown resolves #Y, #X, X; Y gets cycle error. Ordering sensitive.
-- out/eval/stats --
Leaks:  44
Freed:  76
Reused: 58
Allocs: 62
Retain: 0

Unifications: 154
Conjuncts:    182
Disjuncts:    0
Notifications: 1

NumCloseIDs: 76
-- out/compile --
--- in.cue
{
  minimal: {
    a: {
      if (〈1;b〉.port == _|_(explicit error (_|_ literal) in source)) {
        port: ""
      }
    }
    b: {
      if (〈1;a〉.port == _|_(explicit error (_|_ literal) in source)) {
        port: ""
      }
    }
  }
  small: {
    #userHostPort: "^(:(?P<port>\\d+))?$"
    p1: {
      #Y: 〈import;regexp〉.FindNamedSubmatch(〈1;#userHostPort〉, 〈0;#X〉.port)
      #X: {
        if (〈1;#Y〉.port == _|_(explicit error (_|_ literal) in source)) {
          port: ""
        }
        if (〈1;#Y〉.port != _|_(explicit error (_|_ literal) in source)) {
          port: (":" + 〈import;strconv〉.FormatInt(〈2;#Y〉.port, 10))
        }
      }
    }
  }
  medium: {
    #userHostPort: "^(:(?P<port>\\d+))?$"
    p1: {
      #Y: 〈import;regexp〉.FindNamedSubmatch(〈1;#userHostPort〉, 〈0;#X〉.port)
      Y: {
        if (〈1;#Y〉.port != _|_(explicit error (_|_ literal) in source)) {
          port: 〈import;strconv〉.Atoi(〈2;#Y〉.port)
        }
      }
      #X: {
        if (〈1;Y〉.port == _|_(explicit error (_|_ literal) in source)) {
          port: ""
        }
        if (〈1;Y〉.port != _|_(explicit error (_|_ literal) in source)) {
          port: (":" + 〈import;strconv〉.FormatInt(〈2;Y〉.port, 10))
        }
      }
    }
  }
  large: {
    #userHostPort: "^((?P<userinfo>[[:alnum:]]*)@)?(?P<host>[[:alnum:].]+)(:(?P<port>\\d+))?$"
    p1: {
      Y: {
        userinfo: "user"
        host: "mod.test"
      }
      X: ((〈0;#X〉.userinfo + 〈0;#X〉.host) + 〈0;#X〉.port)
      #X: {
        if (〈1;Y〉.userinfo == _|_(explicit error (_|_ literal) in source)) {
          userinfo: ""
        }
        if (〈1;Y〉.userinfo != _|_(explicit error (_|_ literal) in source)) {
          userinfo: (〈2;Y〉.userinfo + "@")
        }
        host: 〈1;Y〉.host
        if (〈1;Y〉.port == _|_(explicit error (_|_ literal) in source)) {
          port: ""
        }
        if (〈1;Y〉.port != _|_(explicit error (_|_ literal) in source)) {
          port: (":" + 〈import;strconv〉.FormatInt(〈2;Y〉.port, 10))
        }
      }
      Y: {
        if (〈1;#Y〉.userinfo != _|_(explicit error (_|_ literal) in source)) {
          userinfo: 〈2;#Y〉.userinfo
        }
        host: 〈1;#Y〉.host
        if (〈1;#Y〉.port != _|_(explicit error (_|_ literal) in source)) {
          port: 〈import;strconv〉.Atoi(〈2;#Y〉.port)
        }
      }
      #Y: 〈import;regexp〉.FindNamedSubmatch(〈1;#userHostPort〉, 〈0;X〉)
    }
  }
}
