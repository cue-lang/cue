# Tests cycle patterns involving default values and range constraints.
-- in.cue --
range1: {
	min:   *1 | int
	range: >min
	range: 8
	@test(eq, {min: *1 | int, range: 8})
}
range2: {
	min: *1 | int
	max: int & >min
	@test(eq, {min: *1 | int, max: >1 & int})
}
rg: range2
rg: {
	//   min: 1
	max: 8
	@test(eq, {max: 8, min: *1 | int})
}
-- out/todo.txt --
rg field ordering differs between v2 and v3: v2 has min before max, v3 has max before min.
-- out/eval/stats --
Leaks:  2
Freed:  14
Reused: 8
Allocs: 8
Retain: 0

Unifications: 10
Conjuncts:    22
Disjuncts:    6

NumCloseIDs: 1
-- out/compile --
--- in.cue
{
  range1: {
    min: (*1|int)
    range: >〈0;min〉
    range: 8
  }
  range2: {
    min: (*1|int)
    max: (int & >〈0;min〉)
  }
  rg: 〈0;range2〉
  rg: {
    max: 8
  }
}
