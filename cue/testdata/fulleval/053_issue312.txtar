#name: issue312
#evalFull
-- in.cue --

for x in [1] {
	*close({}) | {[_]: null}
}
-- out/compile --
--- in.cue
{
  for _, x in [
    1,
  ] {
    (*close({})|{
      [_]: null
    })
  }
}
-- out/evalalpha --
(struct){ |(*(#struct){
  }, (struct){
  }) }
