#name: issue312
#evalFull
-- in.cue --

for x in [1] {
	*close({}) | {[_]: null}
}
-- out/eval --
(struct){ |(*(#struct){
  }, (struct){
  }) }
-- out/compile --
--- in.cue
{
  for _, x in [
    1,
  ] {
    (*close({})|{
      [_]: null
    })
  }
}
