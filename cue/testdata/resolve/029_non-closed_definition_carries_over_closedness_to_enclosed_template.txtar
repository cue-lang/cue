-- in.cue --
#S: {
	[string]: {a: int}
} @test(eq, {}) @test(closed)
a: #S & {
	v: {b: int}
} @test(err, code=eval, contains="field not allowed", pos=[1:6])
#Q: {
	[string]: {a: int} | {b: int}
} @test(eq, {}) @test(closed)
b: #Q & {
	w: {c: int}
} @test(err, code=eval, contains="field not allowed", pos=[1:6])
#R: {
	[string]: [{a: int}, {b: int}]
} @test(eq, {}) @test(closed)
c: #R & {
	w: [{d: int}, ...]
} @test(err, code=eval, contains="field not allowed", pos=[1:7])
-- out/errors.txt --
[eval] a.v.b: field not allowed:
    ./in.cue:5:6
[eval] b.w.c: field not allowed:
    ./in.cue:11:6
[eval] c.w.0.d: field not allowed:
    ./in.cue:17:7
-- out/compile --
--- in.cue
{
  #S: {
    [string]: {
      a: int
    }
  }
  a: (〈0;#S〉 & {
    v: {
      b: int
    }
  })
  #Q: {
    [string]: ({
      a: int
    }|{
      b: int
    })
  }
  b: (〈0;#Q〉 & {
    w: {
      c: int
    }
  })
  #R: {
    [string]: [
      {
        a: int
      },
      {
        b: int
      },
    ]
  }
  c: (〈0;#R〉 & {
    w: [
      {
        d: int
      },
      ...,
    ]
  })
}
-- out/eval/stats --
Leaks:  0
Freed:  24
Reused: 12
Allocs: 12
Retain: 0

Unifications: 20
Conjuncts:    33
Disjuncts:    2

NumCloseIDs: 3
-- out/todo.txt --
Missing error positions.
