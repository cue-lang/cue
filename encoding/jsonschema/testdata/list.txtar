-- list.yaml --
type: object

properties:
  foo:
    type: array
    items:
      type: string

  tuple:
    type: array
    items:
      - type: string
      - type: integer
      - const: 2

  has:
    type: array
    contains:
      const: 3

  size:
    type: array
    minItems: 3
    maxItems: 9
    uniqueItems: true

additionalProperties: false

-- out.cue --
import "list"

Schema :: {
	foo?: [...string]
	tuple?: [string, int, 2]
	has?:  list.Contains(3)
	size?: list.MinItems(3) & list.MaxItems(9) & list.UniqueItems()
}
