# Test that we can use a "number" type constraint with a numeric enum.
# See issue #3173.

# TODO fix this so it works.

-- type.json --
{
    "type": "number",
    "enum": [ 1, 2, 3]
}
-- out/decode/cue --
1 | 2 | 3
