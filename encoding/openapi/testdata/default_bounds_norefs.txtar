# The old encoder with expanded references, as reported in the issue.
# TODO: the conjunction of the non-default arm arrives unsplit and is not
# understood; only the default should be dropped, keeping the bounds.
# See https://cuelang.org/issue/4305
#ExpandReferences
#ExpectError: unsupported op for number &
-- in.cue --
#Bounded:         *20 | int & >=1 & <=100
#TrailingDefault: int & >=1 | *5
