# Test that the plugin provides the language version variables, by checking
# that the version used by `cue mod init` matches CUE_LANGUAGE_VERSION.
plugin cue

exec cue mod init example.com/main@v0
grep '^language: version: "'$CUE_LANGUAGE_VERSION'"$' cue.mod/module.cue
