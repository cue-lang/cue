-- in.cue --
#S: {
	[string]: {a: int}
}
a: #S & {
	v: {b: int}
} @test(err, at=v.b, code=eval, contains="field not allowed", pos=[1:6])
-- out/compile --
--- in.cue
{
  #S: {
    [string]: {
      a: int
    }
  }
  a: (〈0;#S〉 & {
    v: {
      b: int
    }
  })
}
