-- in.cue --
t1: a: {
	foo?: int

	b: foo @test(err, code=incomplete)
}
t2: a: {
	b: foo @test(err, code=incomplete)

	foo?: int
}
-- out/compile --
--- in.cue
{
  t1: {
    a: {
      foo?: int
      b: 〈0;foo〉
    }
  }
  t2: {
    a: {
      b: 〈0;foo〉
      foo?: int
    }
  }
}
