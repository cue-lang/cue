# Tests that a pattern constraint [_]: _ over an open struct results in
# an open struct without any concrete fields.
-- in.cue --
a: {[_]: _}   @test(eq, {})
-- out/compile --
--- in.cue
{
  a: {
    [_]: _
  }
}
