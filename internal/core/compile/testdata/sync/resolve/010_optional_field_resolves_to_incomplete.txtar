-- in.cue --
r: {
	a?: 3
	b:  a      @test(err, code=incomplete, contains="cannot reference optional field: a", pos=[0:6])
	c:  r["a"] @test(err, code=incomplete, contains="cannot reference optional field: a", pos=[0:8])
}
-- out/compile --
--- in.cue
{
  r: {
    a?: 3
    b: 〈0;a〉
    c: 〈1;r〉["a"]
  }
}
