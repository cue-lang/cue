-- cue.mod/modules.cue --
-- file1.cue --
@extern("test")

package scopetest

#Bar: float
#Baz: {
	#Quux: string
	spam: eggs: bool
}

foo: _ @test(int)
bar: _ @test(#Bar)
baz: {
	quux: _ @test(#Baz.#Quux)
	eggs: _ @test(#Baz.spam.eggs)
}
-- out/extern --
{
	foo: int
	bar: float
	baz: {
		quux: string
		eggs: bool
	}
}
