# A non-same-line comment between the operator and the RHS of a
# binary expression stays before the RHS, not after it.
-- in.cue --
x: a + // xx
    // yy

   b
-- out/relpos --
x: a + // xx
	// yy
	b
-- out/norelpos --
x: a + // xx
	// yy
	b
