#exp:all

-- cue.mod/module.cue --
module: "all.test"

language: version: "v0.15.0"

-- a.cue --
package foo

#A: a: int

X: {
	#A
}
#Y: { #A }
-- out/fixmod --
--- cue.mod/module.cue
module: "all.test"
language: {
	version: "v0.15.0"
}
--- a.cue
@experiment(explicitopen)

package foo

#A: a: int

X: __closeAll({
	#A...
})
#Y: {#A...}
