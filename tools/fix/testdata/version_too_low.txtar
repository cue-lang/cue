#exp:explicitopen

-- cue.mod/module.cue --
module: "early.test"

language: version: "v0.14.0"

-- a.cue --
package foo

#A: a: int

X: {
	#A
}
-- out/fixmod --
fix: experiment "explicitopen" requires version v0.15.0 or later, got v0.14.0
--- cue.mod/module.cue
module: "early.test"
language: {
	version: "v0.14.0"
}
--- a.cue
package foo

#A: a: int

X: {
	#A
}
