If we bring out the arguments to a function call, then we can simplify them.
Compare to 27.

-- a.cue --
let x = {a: 5, a: int}
y: len(x)
-- out/trim-v3 --
== a.cue
let x = {a: 5}
y: len(x)
-- out/trim --
== a.cue
let x = {a: 5}
y: len(x)
